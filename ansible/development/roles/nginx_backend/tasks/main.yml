---
# tasks file for nginx

- name: Install Nginx
  ansible.builtin.apt:
    name: nginx
    state: present

- name: Configure Nginx
  ansible.builtin.template:
    src: ./files/backend_nginx.conf.j2
    dest: /etc/nginx/nginx.conf
    mode: "0555"
  notify: Restart nginx
